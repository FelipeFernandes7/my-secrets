import { styled } from "styled-components";

export const FeelingBtn = styled.button`
  width: 30%;
  color: #fff;
  font-size: 0.95rem;
  height: 3rem;
  border: 2px solid #3d3d3d;
  border-radius: 1rem;
  cursor: pointer;
  transition: all linear 0.3s;
  &:active {
    transform: scale(0.95);
  }
`;
